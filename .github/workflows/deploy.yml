name: Deploy Saldoran Sentinel

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Pull and restart service on VPS
        run: |
          # Уведомление о старте деплоя
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_ADMIN_ID }} \
            -d text="🚀 Начат деплой Saldoran Sentinel на сервере 185.233.45.95..."

          ssh -o StrictHostKeyChecking=no root@185.233.45.95 << 'EOF'
            set -e
            cd /home/SaldoranSentinelBot
            git fetch origin
            git reset --hard origin/main
            source venv/bin/activate
            pip install --upgrade pip
            pip install -r requirements.txt
            sudo systemctl restart saldoran-sentinel
            echo "✅ Saldoran Sentinel перезапущен успешно"
          EOF
